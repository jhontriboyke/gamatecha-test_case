const USER_MODELS = require("./user.models");
const CAFE_MODELS = require("./cafe.models");
const MENU_MODELS = require("./menu.models");

module.exports = {
  USER_MODELS,
  CAFE_MODELS,
  MENU_MODELS,
};
